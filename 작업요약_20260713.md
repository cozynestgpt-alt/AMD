# 작업요약 (2026-07-13)

## 완료된 작업

### PR #36 — CP949 버그 수정: .bat 파일 ASCII 통일 + 한글 메시지 EncodedCommand 전환
- 원인: `0a659c7`(NAS 배포를 robocopy → git pull로 전환)에서 추가된 내부 헬퍼가
  `chcp 65001` + UTF-8 한글 텍스트 조합으로 저장되어, 한국어 Windows 콘솔(기본
  CP949)에서 배치 파일이 깨지는 문제. cmd.exe는 배치 파일을 읽을 때 실행 시점
  콘솔의 코드페이지로 바이트를 해석하는데, 이 값이 파일 인코딩과 어긋나면
  `if (...)` 같은 괄호 블록 안에서 한 줄이 엉뚱하게 여러 명령으로 쪼개져 실행됨.
  `chcp 65001`을 넣어도 근본적으로 막지 못함(로컬 재현 테스트로 확인).
- 수정: 내부 헬퍼를 `_최신코드자동확인.bat` → `_auto_git_pull.bat`으로 개명하고
  본문을 순수 ASCII로 재작성. `실행.bat`/`DB업데이트만.bat`/`보고서만생성.bat`/
  `전년대비보고서만생성.bat`의 `call` 대상도 새 ASCII 파일명으로 수정.
  팀원에게 보여줄 한글 알림 메시지는 PowerShell `-EncodedCommand`(Base64/
  UTF-16LE)로 출력해 `.bat` 본문의 ASCII 상태를 유지.
- `docs/legacy/cp949_조사결과_및_결정.md`에 원인·재현 테스트·결정 사항 문서화,
  `CLAUDE.md`에 ".bat 파일 작성 규칙" 절 추가.

### PR #37 — CLAUDE.md: NAS git 재도입 정책 문서 갱신
- 2026-07-09(PR #21)에 "NAS는 git 저장소 아님, robocopy로만 배포"로 정했던
  정책을 2026-07-13(PR #36)에 의도적으로 재검토해 뒤집은 것을 문서에 반영.
- robocopy → git pull 자동화로 다시 바꾼 이유(수동 배포 번거로움)와 안전장치
  (`.pulling.lock` 동시접근 방지, `git pull --ff-only`, pull 실패해도 실행 차단
  안 함, `input/output/DB/backup`은 `.gitignore`로 보호)를 `CLAUDE.md`에 명시.
- 실제 NAS UNC 경로에서 `git pull --ff-only`를 직접 실행해 `ea7ffb2` →
  `37acf8e`까지 fast-forward 성공, 데이터 폴더 미접근까지 검증 완료.

### PR #38 — NAS git pull dubious ownership(CVE-2022-24765) 자가 치유 로직 추가
- 실기기 검증 중 실제 팀원 PC에서 `실행.bat` 더블클릭 시 "[알림] 최신 코드
  확인 실패"가 재현됨. 원인은 Git 2.35.2+의 dubious ownership 보호 기능 —
  NAS(SMB 공유)의 소유자 SID가 로그인 계정과 안 맞아 pull이 거부되고 있었음.
- 수정: `_pull_retry.ps1`(신규, 평문 UTF-8 BOM)을 추가해 `git config
  --fixed-value --get-all safe.directory`로 이 NAS 경로가 이미 등록됐는지
  git 자신에게 물어보고, 없으면 **이 경로 하나만**(`*` 전체 신뢰 아님) 등록한
  뒤 pull을 한 번 더 재시도. `_auto_git_pull.bat`은 이 `.ps1`을 `-File`로
  호출하는 한 줄로 단순화(여러 줄짜리 로직을 Base64 한 줄에 욱여넣으면 감사
  불가능하다고 판단해 평문 파일로 분리).
- 개발 중 PowerShell `-notcontains` 문자열 비교가 유니코드 정규화 차이로
  추정되는 이유로 동일 값을 중복 등록하는 버그를 발견해 `--fixed-value`
  방식으로 교체.
- 실제 NAS UNC 경로에서 전체 흐름(최초 등록 → pull 재시도 성공, 재실행 시
  중복 추가 없음) 검증 완료.

### PR #39 — 한글 알림 메시지 실기기 검증 완료로 문서 갱신
- PR #38 조사 중 마침 dubious ownership 실패 상황이 실제로 발생했고, 이때
  콘솔 화면에 한글 알림 메시지가 깨짐 없이 정상 출력되는 것을 육안으로 확인.
- `docs/legacy/cp949_조사결과_및_결정.md` 5번 항목("실기기 미검증")과
  `CLAUDE.md` 두 곳을 "검증 완료"로 갱신.

### PR #40 — CLAUDE.md: NAS docs 폴더 보관 원칙 정정 (git pull 전환 반영)
- NAS `docs/` 동기화 상태를 확인하던 중, 원래 "GitHub 전용"으로 문서화돼
  있던 Git/PR 워크플로우 매뉴얼 2건(`Claude_Code_GitHub_시작_매뉴얼.docx`,
  `main_브랜치_보호_PR_워크플로우_매뉴얼.docx`)이 이미 NAS에도 존재함을 발견.
- 원인: 예전 robocopy 배포는 `docs/`를 애초에 복사 대상에 넣지 않아 NAS의
  `docs/`가 수동 관리되던 별개 폴더였음. git pull 방식으로 바뀌며 `docs/`
  전체(관리자용 문서 포함)가 자동 동기화되기 시작해 이 전제가 깨짐.
  `CLAUDE.md`의 관련 문구를 실제 동작과 일치하도록 정정.

### NAS 실제 반영 확인
- 위 5개 PR 병합 후, 로컬 main과 NAS git 클론 양쪽 모두 최신 커밋
  (`76a17e9`)까지 일치함을 실제 `git log`/`git pull --ff-only`로 확인.

## 남은 사항

- [ ] 없음 — 오늘 다룬 CP949/NAS git pull 전환 관련 항목(.bat 인코딩, NAS git
      재도입, dubious ownership, 한글 메시지 실기기 검증, NAS docs 폴더 동기화)
      전부 실기기/실제 NAS 환경 검증까지 완료된 상태로 마무리됨.
