# 작업요약 (2026-07-10)

## 완료된 작업

### PR #23 — 매출집계_분석.xlsx 27행(00143 0행) DB 반영 제외
- 원인: 00143 롯데잠실캐슬프라자점은 행사매장으로, 거래가 없는 달엔 판매금액~순이익
  7개 지표가 전부 0인 행이 됨. `sales_analysis.py`의 정상 동작이지만, 이 빈 행이
  그대로 `DB/월별손익DB.xlsx`에 섞여 랭킹/집계 시트를 왜곡할 수 있었음.
- 수정: `history_update.py::read_analysis()`에서 7개 지표가 모두 0인 행을 DB 반영
  대상에서 제외.
- 로컬 DB, NAS DB 모두 2026-06 기준으로 재실행해 00143 0행 정리 완료.
- NAS 배포 완료.

### PR #24 — 경영분석보고서 2단계: 시트 재설계
- 삭제(6개): 전년동기대비, 동일매장분석, 행사매장분석, 폐점매장분석, 신규매장분석, 분기분석
  (관련 전용 함수·EVENT_INFO/CLOSED_INFO 등 죽은 코드도 함께 정리)
- `영업이익높은순` → `월정산보고`로 전면 교체: 원 단위, 매장코드순, 영업이익(V-) 기준
  순이익율/경비율, 합계행 + 총매출/매장수/평균매출 집계행
- `경비율높은순`: 원 단위 11열로 재구성, 총경비율 내림차순, 합계행 추가
- `순이익낮은순`: 단일월 랭킹표 → 최근 12개월 매장별 순이익 추이표로 재설계
- `월별경비율추이`: 산식을 총경비/매출 → 총경비/영업이익(V-)로 변경, 12개월 창으로 축소
- 최종 시트 순서: Dashboard → 월정산보고 → 연도별월별영업순이익 → 경비율높은순 →
  순이익낮은순 → 월별경비율추이 → 시즌분석 (7개)
- `templates/` 아래 참고용 템플릿 5개 함께 커밋
- NAS 배포 완료.

### PR #25 — 연도별월별영업순이익 시트 버그 3건 수정
2단계 검증 중 발견된, `add_year_month_profit()` 자체에 있던 기존 버그(2단계에서
새로 만든 게 아님):
1. **누적표 미래월 오염**: 아직 지나지 않은 달에도 직전월 누적값이 그대로 반복
   표시되던 버그 → 미래월 판정을 값 기준이 아니라 (년,월) vs 정산 대상월 기준으로 변경
2. **데이터 레이블 없음**: 막대 차트에 레이블이 전혀 설정돼 있지 않았음 → 최근 연도
   계열에만 데이터 레이블(#,##0, 막대 바깥쪽) 표시하도록 추가
3. **X축/Y축 눈금 라벨 렌더링 안 됨**: 카테고리축·값축의 axPos가 둘 다 'l'로 충돌하고
   delete가 미지정이라 Excel이 눈금 자체를 그리지 않던 문제 → axPos 분리(b/l) +
   tickLblPos='nextTo' + delete=False 명시로 해결 (Excel COM으로 실제 렌더링해
   delete=False가 근본 원인임을 최소 재현으로 확인)
- 격리 DB 사본 + Excel COM 실제 렌더링으로 X축/Y축/데이터 레이블/누적표 4개 항목 재검증,
  워크북 전체 수식 오류 0건 확인
- **2026-07-10 GitHub에서 병합 완료.**

## 다음에 할 일

- [ ] **NAS 배포 필요**: PR #25 반영분(`management_report.py`)이 아직 NAS에
      배포되지 않음. `1_배포_NAS로_동기화.bat` 방식으로 반영 필요.
- [ ] **경영분석보고서 2단계 나머지 시트 템플릿 대조 검증 미착수**: PR #24에서
      템플릿 스펙대로 구현은 했지만, `연도별월별영업순이익` 시트처럼 실제 Excel
      렌더링으로 세밀하게 재검증한 건 아직 없음. 아래 4개 시트는 검증 예정:
      - 월정산보고
      - 경비율높은순
      - 순이익낮은순
      - 월별경비율추이
- [ ] **2023~2025년 과거 데이터 정확성 검증** (별도 작업, 이번 범위 아님): 참고
      템플릿(`templates/연도별월별영업순이익.png`)의 2023년 4월 값(496,052,210원)과
      `DB/월별손익DB.xlsx` 원본 합산값(508,369,000원)이 다름. DB 원본 기준으로는
      508,369가 정답으로 확인됐지만, 템플릿 쪽 496,052,210의 근거는 아직 불명.
      2026-05 이전 데이터는 기존 DB 보존 정책 대상이라 이번 작업 범위에서
      제외했음 — 별도로 다룰 것.
